# ===========================================================================
# Construction Portal — Frontend environment template
#
# Copy to .env and fill in:   cp .env.example .env
#
# WARNING: every VITE_* variable is embedded into the public JS bundle.
# Never put a secret, service-role key, or password in this file.
# ===========================================================================

VITE_API_URL=http://localhost:5051/api
